import { SmplToUsdPipe } from './smpl-to-usd.pipe';

describe('SmplToUsdPipe', () => {
  it('create an instance', () => {
    const pipe = new SmplToUsdPipe();
    expect(pipe).toBeTruthy();
  });
});
